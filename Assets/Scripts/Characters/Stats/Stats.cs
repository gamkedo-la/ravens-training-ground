using Unity.VisualScripting;
using UnityEngine;

namespace Character.Stats
{
    public enum Stat {
        ExperienceGainedForDefeat,
        ExperienceRequiredForLevel,
        Health,
        Magic,
        Physical,
        Agility,
        Finesse
    }
}